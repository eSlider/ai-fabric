# Issue #1 execution

Type: feature
Title: [feature] Telegram /task smoke

## Task
Implement the issue end-to-end in this repository:
- write or update tests first where applicable
- implement minimal safe changes
- run `./bin/fmt.sh && ./bin/lint.sh && ./bin/test.sh`
- commit changes
- push branch

## Issue body
## Type
feature

## Goal
Validate bot issue creation path.

## Value
CI confidence.

## Acceptance Criteria
- issue can be created via API

<!-- ai-fabric:solution-architect:start -->
## Solution Architect
- Type classification: `feature`
- Suggested skills/context: `docs/skills/agent-guidelines.md`, `docs/skills/solution-architect.md`, `docs/skills/developer.md`, `docs/workflows/ci-cd.md`, `README.md`, `docs/workflows/pr-best-practices.md`

## Possible Solutions
- Option A: Add a CI smoke test that directly calls the same issue-creation API path used by Telegram `/task` flow (service-level integration), then asserts a new issue exists in target repo.
- Option B: Add full end-to-end Telegram-driven smoke (send `/task` update to bot webhook/poller, simulate follow-up answers, verify issue is created in Gitea).

## Recommended Approach
- Why this option is preferred
  - Prefer Option A: faster, deterministic, lower flake risk, and directly matches acceptance criteria (`issue can be created via API`) while still validating core integration path.
- Risks
  - False confidence if Telegram adapter layer breaks while API path remains healthy.
  - Requires careful isolation/cleanup to avoid polluting issue tracker with test artifacts.
- Dependencies
  - Valid bot/API credentials and repo target env vars in CI (`GITEA_BOT_BASE_URL`, `GITEA_BOT_OWNER`, `GITEA_BOT_REPO`, `GITEA_BOT_TOKEN`).
  - Reachable Gitea service in CI job.
  - Existing CI gates and scripts (`bin/test.sh` / workflow job wiring).

## Estimation
- Complexity: M
- Estimated effort: 0.5-1 day
- Test scope: Add one integration smoke test for create-issue API call, success response validation, and created-issue verification (with cleanup or unique marker strategy).

## Impact
- User impact
  - Higher confidence that `/task` backend path can create issues reliably.
- Delivery impact
  - Adds a focused CI signal for regression detection before merge.
- Operational impact
  - Minimal runtime overhead; small increase in CI setup complexity for secrets/test data handling.

## Required Skills/Context
- `README.md`
- `docs/skills/agent-guidelines.md`
- `docs/skills/solution-architect.md`
- `docs/skills/developer.md`
- `docs/workflows/ci-cd.md`
- `docs/workflows/pr-best-practices.md`
<!-- ai-fabric:solution-architect:end -->

## Relevant skills/docs to read first
- `docs/skills/agent-guidelines.md`
- `docs/skills/solution-architect.md`
- `docs/skills/developer.md`
- `docs/workflows/ci-cd.md`
- `README.md`
- `docs/workflows/pr-best-practices.md`

## Constraints
- Keep changes scoped to this issue
- Follow PR template and workflow policies
- If CI fails, fix and retry until green within attempt limit
